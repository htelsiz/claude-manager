[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "cchooks"
version = "0.2.0"
description = "Claude Code hook SDK — Nix-packaged fork of GowayLee/cchooks"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
